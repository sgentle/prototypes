var toflowtype = require('./index.js');

console.log(toflowtype("a function that takes an array of string and an optional number and returns a number"));

console.log(toflowtype("a function that takes an optional array of array of string and returns an array of array of optional string"));